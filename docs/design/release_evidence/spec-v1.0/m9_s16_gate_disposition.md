# M9 S16 corpus-assembly gate disposition

Production S16 is **not authorized**. GLM is complete and sealed through S15,
but it is only one input component of corpus-wide S16; a GLM-only independent
split must not be described as conformant S16.

Five independent reviewer voices—DeepSeek Flash, DeepSeek Pro, Gemini, Kimi,
and the smaller Codex Luna model—agreed on the substantive disposition. The
reconciled verdict is **approve after fixes**. The complete review evidence is
bound to align commit `f43dd7ae694a79e8dec101bce59266e394014716` and the
hashes in the adjacent JSON record.

The immediate implementation-owned next step is Qwen: reconstruct and review
its legacy run provenance, then run deterministic S12–S15 sealing only after
that provenance is explicitly accepted. Production S16 remains blocked until a
complete additional generator-family lane exists and BM/specification authority
resolves the N6.6 × N7.1 CAL-50 conflict and freezes every corpus-wide split,
scene, qtype, hard-core, held-out-family, and OOD-Dataset contract.

This record is a stop/go disposition, not a waiver of any missing evidence or a
future-output acceptance.
