"""Component package `release` — see Implementation Architecture §2.

Skeleton only until implementation authorization (V1-044); every future
module here must trace its stage obligations to the V1-040 acceptance
matrix rows that bind it.
"""
